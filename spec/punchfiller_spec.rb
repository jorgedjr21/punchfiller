# frozen_string_literal: true

RSpec.describe Punchfiller do
  it "has a version number" do
    expect(Punchfiller::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
